export function increaser(a, index) {
    if (a > index) {
        a += index;
        // console.log(a);
        return a;
    } else {
        return a;
    }
   
}

// console.log(increaser(10, 5));